services:
  - type: web
    name: micro-inventario
    env: docker
    plan: free
    region: oregon
    dockerfilePath: ./Dockerfile
    envVars:
      - key: PROJECT_PATH
        value: microPedidos.API
      - key: PROJECT_DLL
        value: microPedidos.API.dll
